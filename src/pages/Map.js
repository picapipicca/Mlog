import React,{ Fragment }  from 'react';
import classes from "./Map.module.css"

const Map = (props) => {
    return (
        <Fragment>
            첫화면/내 버스와 다른사람들 버스가 보이는 페이지!
        </Fragment>
    );
};

export default Map;