import React, { Fragment } from "react";

const Bus = (props) => {
    return(
        <Fragment>
          내버스 ,  화면에서 보이는 버스들 (클릭시 그사람 제일 최근 일기로 이동함)
        </Fragment>
    );
};

export default Bus;