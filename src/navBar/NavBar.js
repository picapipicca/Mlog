import React, { Fragment } from 'react';
import classes from "./NavBar.module.css";

const NavBar = (props) => {
    return (
        <Fragment>
            
        </Fragment>
    );
};

export default NavBar;